# LaboratorioRAG_AWS

añadir configuracion
aws configure
-> json como salida por defecto

Listar los workspace
terraform workspace list

Crear un workspace
terraform workspace new dev

Seleccionar o cambiar de Workspace
terraform workspace select dev

saber el workspace actual
terraform workspace show

Eliminar 
terraform workspace delete dev
